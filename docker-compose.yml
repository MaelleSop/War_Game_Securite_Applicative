version: '3.8'

services:
  web:
    build: .
    ports:
      - "8080:80"
    volumes:
      - .:/var/www/html
      - db_data:/var/www/html/uploads
    networks:
      - app-network

volumes:
  db_data:

networks:
  app-network:
    driver: bridge
